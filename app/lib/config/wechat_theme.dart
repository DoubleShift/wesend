/// WeChat-style color constants
class WeChatColors {
  WeChatColors._();

  /// Primary green (#07C160)
  static const primary = Color(0xFF07C160);

  /// Lighter green for chat bubbles
  static const bubbleSent = Color(0xFF95EC69);

  /// Background color
  static const background = Color(0xFFF7F7F7);

  /// Card / surface color
  static const surface = Color(0xFFFFFFFF);

  /// Primary text
  static const textPrimary = Color(0xFF191919);

  /// Secondary text
  static const textSecondary = Color(0xFF999999);

  /// Divider color
  static const divider = Color(0xFFE6E6E6);

  /// Tab bar indicator / active tab
  static const tabActive = Color(0xFF07C160);

  /// Unselected tab text
  static const tabInactive = Color(0xFF8A8A8A);

  /// Chat bubble received (other side)
  static const bubbleReceived = Color(0xFFFFFFFF);

  /// App bar background
  static const appBar = Color(0xFFEDEDED);

  /// Search bar background
  static const searchBg = Color(0xFFEDEDED);
}
